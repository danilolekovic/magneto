/*
 *  ' lexerTests.js '
 *
 *      lexical analysis tests
 */

import Test from "./test.mjs"
import Lexer from "../lexer.mjs"

export class LexerTests extends Test {
    constructor(left, right, name) {
        super(left, right, name);
    }

    run() {
        // 'left' is an array of tokens
        // 'right' is the output we expect
    }
};